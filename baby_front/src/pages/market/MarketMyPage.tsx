import BasicLayout from "../../layouts/BasicLayout";
import MarketMyConponent from "../../components/market/MarketMyComponent";

// 마켓 내부 마이페이지
const MarketMyPage = () => {
  return (
    <BasicLayout>
      <MarketMyConponent />
    </BasicLayout>
  );
};

export default MarketMyPage;
