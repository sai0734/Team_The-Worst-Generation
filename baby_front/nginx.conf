server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # react-router 같은 클라이언트 사이드 라우팅 대응:
        # 실제 파일이 없으면 index.html로 넘겨서 프론트 라우터가 처리하게 함
        try_files $uri $uri/ /index.html;
    }
}
