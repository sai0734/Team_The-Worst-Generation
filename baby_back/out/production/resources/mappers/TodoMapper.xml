<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.mapper.TodoMapper">

    <select id="selectList" resultType="com.backend.domain.Todo">
        SELECT tno, title, writer, complete, due_date
        FROM tbl_todo
        ORDER BY tno DESC
        LIMIT #{size} OFFSET #{skip}
    </select>

    <select id="selectListCount" resultType="long">
        SELECT COUNT(*) FROM tbl_todo
    </select>

    <select id="selectByTno" resultType="com.backend.domain.Todo">
        SELECT tno, title, writer, complete, due_date
        FROM tbl_todo
        WHERE tno = #{tno}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="tno" keyColumn="tno">
        INSERT INTO tbl_todo (title, writer, complete, due_date)
        VALUES (#{title}, #{writer}, #{complete}, #{dueDate})
    </insert>

    <update id="update">
        UPDATE tbl_todo
        SET title = #{title},
            complete = #{complete},
            due_date = #{dueDate}
        WHERE tno = #{tno}
    </update>

    <delete id="delete">
        DELETE FROM tbl_todo WHERE tno = #{tno}
    </delete>

</mapper>
