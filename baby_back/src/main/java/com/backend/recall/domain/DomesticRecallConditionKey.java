package com.backend.recall.domain;

/**
 * SafetyKorea 국내리콜정보 조회 API의 conditionKey 값.
 */
public enum DomesticRecallConditionKey {
    all, barcodeNum, recallProductName, recallBrandName, recallModelName, certNum, publishDate;
}
