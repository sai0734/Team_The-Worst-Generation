package com.backend.recall.domain;

/**
 * SafetyKorea 국외리콜정보 조회 API의 conditionKey 값.
 */
public enum ForeignRecallConditionKey {
    all, recallProductName, recallBrandName, recallModelName, publishDate, fRecallUid;
}
