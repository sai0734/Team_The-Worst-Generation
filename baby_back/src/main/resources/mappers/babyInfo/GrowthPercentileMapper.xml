<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.babyInfo.mapper.GrowthPercentileMapper">

    <select id="selectByAgeAndGender" resultType="com.backend.babyInfo.domain.GrowthPercentile">
        SELECT percentile_no, age_month, gender, avg_weight_kg, avg_height_cm
        FROM tbl_growth_percentile
        WHERE age_month = #{ageMonth} AND gender = #{gender}
    </select>

</mapper>