<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.babyInfo.mapper.BabyGrowInfoMapper">

    <select id="selectList" resultType="com.backend.babyInfo.domain.BabyGrowInfo">
        SELECT g.baby_grow_no, g.baby_no, g.measured_date, g.weight, g.height, g.reg_time
        FROM tbl_baby_grow_info g
        JOIN tbl_baby_info b ON g.baby_no = b.baby_no
        WHERE g.baby_no = #{babyNo} AND b.email = #{email}
    </select>

    <select id="selectByBabyGrowNo" resultType="com.backend.babyInfo.domain.BabyGrowInfo">
        SELECT g.baby_grow_no, g.baby_no, g.measured_date, g.weight, g.height, g.reg_time
        FROM tbl_baby_grow_info g
        JOIN tbl_baby_info b ON g.baby_no = b.baby_no
        WHERE g.baby_grow_no = #{babyGrowNo} AND b.email = #{email}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="babyGrowNo" keyColumn="baby_grow_no">
        INSERT INTO tbl_baby_grow_info (baby_no, measured_date, weight, height)
        VALUES (#{babyNo}, #{measuredDate}, #{weight}, #{height})
    </insert>

    <delete id="remove">
        DELETE g FROM tbl_baby_grow_info g
        JOIN tbl_baby_info b ON g.baby_no = b.baby_no
        WHERE g.baby_grow_no = #{babyGrowNo} AND b.email = #{email}
    </delete>

    <delete id="removeByBabyNo">
        DELETE g FROM tbl_baby_grow_info g
        JOIN tbl_baby_info b ON g.baby_no = b.baby_no
        WHERE g.baby_no = #{babyNo} AND b.email = #{email}
    </delete>

</mapper>