<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.aibeHavior.mapper.BabyBehaviorMessageMapper">

    <select id="selectListByConsultNo" resultType="com.backend.aibeHavior.domain.BabyBehaviorMessage">
        SELECT message_no, consult_no, role, content, reg_time
        FROM tbl_baby_behavior_message
        WHERE consult_no = #{consultNo}
        ORDER BY reg_time ASC, message_no ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="messageNo" keyColumn="message_no">
        INSERT INTO tbl_baby_behavior_message (consult_no, role, content)
        VALUES (#{consultNo}, #{role}, #{content})
    </insert>

    <delete id="deleteByConsultNo">
        DELETE FROM tbl_baby_behavior_message WHERE consult_no = #{consultNo}
    </delete>

</mapper>