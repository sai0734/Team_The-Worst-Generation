<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.auth.mapper.MemberRefreshTokenMapper">

    <resultMap id="memberRefreshTokenResultMap" type="com.backend.auth.domain.MemberRefreshToken">
        <id property="id" column="id"/>
        <result property="memberEmail" column="member_email"/>
        <result property="sessionId" column="session_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="revoked" column="revoked"/>
        <result property="usedAt" column="used_at"/>
        <result property="userAgent" column="user_agent"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByTokenHash" resultMap="memberRefreshTokenResultMap">
        SELECT id,
        member_email,
        session_id,
        token_hash,
        expires_at,
        revoked,
        used_at,
        user_agent,
        created_at
        FROM tbl_member_refresh_token
        WHERE token_hash = #{tokenHash}
    </select>

    <insert id="insert">
        INSERT INTO tbl_member_refresh_token (
        member_email,
        session_id,
        token_hash,
        expires_at,
        user_agent
        )
        VALUES (
        #{memberEmail},
        #{sessionId},
        #{tokenHash},
        #{expiresAt},
        #{userAgent}
        )
    </insert>

    <update id="markUsed">
        UPDATE tbl_member_refresh_token
        SET used_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="revokeByTokenHash">
        UPDATE tbl_member_refresh_token
        SET revoked = TRUE
        WHERE token_hash = #{tokenHash}
    </update>

    <update id="revokeAllBySessionId">
        UPDATE tbl_member_refresh_token
        SET revoked = TRUE
        WHERE session_id = #{sessionId}
    </update>

</mapper>