<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.community.mapper.CommunityPostLikeMapper">

    <select id="selectOne" resultType="com.backend.community.domain.CommunityPostLike">
        SELECT like_no, post_no, member_email, reg_time
        FROM tbl_community_post_like
        WHERE post_no = #{postNo} AND member_email = #{memberEmail}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="likeNo" keyColumn="like_no">
        INSERT INTO tbl_community_post_like (post_no, member_email)
        VALUES (#{postNo}, #{memberEmail})
    </insert>

    <delete id="delete">
        DELETE FROM tbl_community_post_like WHERE post_no = #{postNo} AND member_email = #{memberEmail}
    </delete>

</mapper>
